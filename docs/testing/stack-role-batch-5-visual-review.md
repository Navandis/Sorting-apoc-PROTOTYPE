# Stack Role Batch 5 Focused Visual Review

`loot_000041` (`SM_Hammer_3.glb`) is the sole Batch 5 visual-review case.
Its approved upright, flipped pose is provisionally `false / false` because it
has too little stable base area for deterministic centered storage.

Run `main.tscn`, place the hammer in its approved pose on an empty shelf, and
inspect whether it can rest centered without leaning or an offset. Do not
treat a narrow, unstable contact as a credible stack relationship.

```text
loot_000041 — APPROVE
loot_000041 — ADJUST: true / false
loot_000041 — HOLD
```
