# Sorting Apocalypse Codex bootstrap

Use the authoritative local project at `D:\Godot Projects\Sorting-apoc-PROTOTYPE`. Verify the branch, working tree, Godot executable and current remote ancestry before modifying anything. Do not reset to a reported SHA or discard editor-authored work.

Read in this order:

1. `docs/AI_WORKING_GUIDELINES.md`
2. `docs/CURRENT_STATE.md`
3. `docs/README.md`
4. The three current DOCX masters or their generated readable extracts
5. Only the validation records relevant to the active gate

Use one executor by default and match verification to risk. Reuse established commands and same-state evidence where appropriate. Do not create a new review hierarchy, evidence framework or broad regression loop for a bounded task.

The normal playable composition is `res://gameplay/logistics_wing/wing_gameplay.tscn`. Its saved `DevelopmentSetup/SeedItems` composition is the palette authority. It contains the human-promoted temporary Gallery B four-level `ModularRack` and independent `FixedLadder` as the real Receiving Stage B storage-destination baseline, plus the human-promoted deterministic seeded TAKE-only Receiving deck presenter. The developer may move/refine these installations later. It must not contain the isolated Receiving A/B/C comparison, physics-pile proof, proof player, proof-only reach, or another experimental Receiving presenter. Normal launch must not synthesize a Receiving delivery; synthetic batches remain explicit debug/CLI behavior. Preserve the legacy `main.tscn`, the neutral wing review scene, the shelf ergonomics review scene, the developer-authored modular-rack experiment and the cleanly isolated Receiving review history.

Review-scene supply reuse, reusable ModularRack authoring, Storage Unit Orientation & Zoning Basis, Canonical Item Storage Pose & Shelf-Front Alignment, the standalone fixed-ladder architecture, and the Gallery B wing rack-and-ladder integration are promoted foundations, not pending work. Fixed ladders remain limited to selected taller storage installations; no general climbing, jumping, movable/sliding ladder or shelf-top traversal system is approved.

Current gate order and Receiving invariants:

1. The deterministic seeded TAKE-only deck presenter is implemented, technically verified and human-promoted in its tested Stage B scope. Future work must preserve it rather than reimplement it.
2. Receiving Stage A remains upstream batch/lifecycle authority. Expedition owns the exact batch contents; Receiving only presents that exact content and may not add filler, remove awkward items, replace large items, or bias Expedition outcomes.
3. Presentation uses private placement surfaces and ordinary `WorldItem`s for TAKE only. No PUT, zoning, labels, manual placement, visible grids, F6 Receiving surface, or ordinary functional-storage registration is allowed.
4. Fill proceeds from local `+Z` FRONT at the player/barrier/apron side toward local `-Z` REAR inside the lift. Seeded occlusion and existing legal support-stack rules are accepted; capacity failure stays explicit and atomic.
5. Normal launch has no synthetic delivery. Receiving-specific reach remains separate from ordinary loose and storage reach.
6. The current approved baseline is **3.00 m × 2.00 m usable**, **3.10 m × 2.10 m support** with a **0.05 m edge border**, and **2.1 m Receiving reach**. These are authorable current values, not eternal constants.
7. **Next active gate: Receiving Stage B follow-on design/refinement, to be separately scoped before implementation.** Potential topics include low functional freight fixtures, reveal/shutter theatre and cue hooks, later capacity sizing, and production-lighting/readability validation. Stage C physical queue pressure remains later work.
8. Do not infer a general climbing or expanded ladder gate from the fixed-ladder promotion.

Do not treat the 1.80 m eye-height, 2.8 m ceiling trials, approximately `3.87 m` proof cage, or proof-only `3.5 m` TAKE reach as production changes. The irregular frozen-pile direction is human-rejected; do not design a support graph to rescue it. Preserve the promoted storage, fixed-ladder and deterministic Receiving-deck foundations rather than reimplementing them. Do not implement freight fixtures, reveal/shutter theatre, final lighting, Stage C queue presentation, gallery furnishing, crouch, height-aware auto-placement, player-adjustable shelves or broader climbing work unless a later handoff explicitly authorizes it.

The GDD v0.10, Prototype Findings v0.10 and VDD v0.7 are dated masters. For current ladder and Receiving status, use `docs/CURRENT_STATE.md` and the focused validation records until the next broader master-document revision.
